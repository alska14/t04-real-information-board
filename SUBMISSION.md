# T04 제출문 초안

## 짧은 확인 방법 (T04-C27)

1. **어디로 가나요**: https://app-zeta-eight-33.vercel.app 접속 (로그인·가입·초대·비밀번호·OAuth·CAPTCHA 없음)
2. **3단계 이내 무엇을 하나요**:
   1) 페이지 열면 "① 실제 정보판"에 비트코인 KRW 시세가 자동 조회되어 뜬다.
   2) "다시 조회" 버튼을 눌러 최신 값을 갱신한다.
   3) "② 합성 재생 데모" 버튼(예: 느린 응답, RECOVER-D2 등)을 눌러 실패·회복 상태 전환을 확인한다.
3. **무엇이 보이면 통과인가요**:
   - ①에서 값·단위(KRW)·출처(CoinGecko 링크)·출처 시각·조회 시각·기준 시간대(Asia/Seoul)가 모두 표시됨
   - 상태 배지가 "fresh · 정상"으로 뜸
   - ②에서 실패 버튼을 누르면 배지가 "stale(오래된 값) · [오류 종류]"로 바뀌고 직전 정상값(예: 100 pt)은 그대로 남아 있음
   - RECOVER-D2를 누르면 "fresh · 정상"으로 돌아오고 일별 기록이 1건 늘어남(2026-08-24 → 2026-08-25 추가)
4. **안 될 때 무엇이 보이나요**:
   - 네트워크가 끊기면 라이브 패널 배지가 "stale · 오프라인"으로 바뀌고 마지막 정상값이 유지됨(값이 지워지거나 0으로 바뀌지 않음)
   - 버튼은 요청 중 "조회 중…/재생 중…"으로 바뀌고 중복 클릭이 막힘

## AI·직접 판단 구분 (T04-C28)

**① AI에게 맡긴 일**
- Next.js(App Router) + Neon Postgres 프로젝트 스캐폴딩, TypeScript 타입/스키마 코드, API 라우트(`/api/reading`, `/api/replay`, `/api/reset`, `/api/receipts`) 구현
- 9개 결정론 fixture를 `adapter-reset.example.js`와 같은 규칙으로 옮겨 DB 트랜잭션 버전으로 재작성
- UI 스타일링(다크 대시보드, 상태 배지, 반응형 테이블)
- GitHub public 레포 생성·커밋·푸시, Vercel 배포, Neon Postgres 프로비저닝

**② 학생이 직접 판단한 일**
- 실제 추적 데이터로 **비트코인 KRW 시세**를 선택 (환율/날씨 등 다른 후보 중 직접 결정)
- 기술 스택: 기존 aleph-omega 프로젝트와 통일성 위해 **Next.js + Vercel** 선택
- DB는 **Vercel 마켓플레이스 Neon Postgres** 선택
- 소스코드 저장소를 **새 GitHub public 레포**로 만들기로 결정
- `제출정보.json.과정영수증[]`은 플랫폼이 자동 생성하지 않는다고 판단해 앱 안에 `/api/receipts`로 직접 만들기로 결정
- Vercel 로그인·Neon 마켓플레이스 약관 동의는 계정 소유자 본인이 브라우저에서 직접 승인 (AI가 대신 로그인/동의하지 않음)

**③ AI 제안을 따르지 않은 일**
- (해당 없음 — AI가 제시한 기술 스택·DB·저장소 선택지를 모두 그대로 채택함. 추후 기능 추가 단계에서 채택하지 않는 제안이 생기면 여기에 기록 예정)

---
*이 파일은 초안입니다. 실제 제출 폼(ALEPH 대시보드)의 텍스트 필드에 옮겨 붙여넣으세요.*
